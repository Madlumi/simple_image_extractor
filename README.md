# simple_image_extractor
extract images in order into a new folder, usefull for animations
Main use is combining multiple shots of rendered animation into a folder to make it easier to ffmpeg into a video
